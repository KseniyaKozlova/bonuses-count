spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bonuses
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  jpa:
    generate-ddl: true
    show-sql: false
    hibernate:
      ddl-auto: none

server:
  port: 8083
